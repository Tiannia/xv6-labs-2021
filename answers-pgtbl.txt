Speed up system calls:
fstat(). In fact, all system calls involving reading data structures can be speed up by sharing data.

Print a page table:
page 0 is the code of the program init.
page 2 is the stack of this process.
page 1 is the guard page which the process could not read/write.
The third to last page contains one page of heap.